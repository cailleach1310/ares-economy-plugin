---
en:
  economy:

        # Success
        econ_block: "%{name} has been blocked from financial ventures for %{days} days. Reason: %{reason}"

        # Error
        econ_blocked: "The economy block is still in effect."
        limit_set: "Your limit is already set!"

        # Debug
        calc_details: "Limit calculated with the following parameters: %{factor} (factor), %{modifier} (overall modifier) and %{random} (random number)."
